//! Workspace-level integration tests live under `tests/tests/*.rs`
//! (cargo's standard integration-test convention). This crate has no
//! library surface of its own — it only exists to host those tests plus
//! shared test fixtures.

pub mod fixtures;
