[package]
name = "telemetry"
version = "0.1.3"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
common = { path = "../common" }
network-state = { path = "../network-state" }
